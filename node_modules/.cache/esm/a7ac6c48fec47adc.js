let Joi,userInfo,CustomErrorHandler,User;_fec‍.x([["default",()=>_fec‍.o]]);_fec‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_fec‍.w("os",[["userInfo",["userInfo"],function(v){userInfo=v}]]);_fec‍.w("../../services/CustomeErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_fec‍.w("../../models",[["User",["User"],function(v){User=v}]]);






const registerController = {

    async register(req,res,next){

         // CHECKLIST
        // [ ] validate the request
        // [ ] authorise the request
        // [ ] check if user is in the database already
        // [ ] prepare model
        // [ ] store in database
        // [ ] generate jwt token
        // [ ] send response

        // Validation
        const registerSchema = Joi.object({
            name: Joi.string().min(3).max(30).required(),
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
            repeat_password: Joi.ref('password')
        });

        
        const {error} = registerSchema.validate(req.body)

        if(error){
            
            return next(error)
            
        }


        //check if user is already exist in my database
        try{

            const exist = await User.exits({email:req.body.email})
            if(exist){

                return next(CustomErrorHandler.alreadyExist("This email is already exist"))
            }

        }

        catch(err){
            
            return next(err)

        }

        res.json({msg:"Hello world"})


    }
}

_fec‍.d(registerController);